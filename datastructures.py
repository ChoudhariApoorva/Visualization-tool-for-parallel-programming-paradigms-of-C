from collections import *
line_num = [1]
local_var = []
local_var_dict = defaultdict(list)
args_dict = defaultdict(int)
func_name_dict = OrderedDict()
info = []
line_num_for_threads = defaultdict(int)
printf_list = []
global_var_list = []
break_point_dict = defaultdict(int)
